
import 'package:flutter/material.dart';

@immutable
class AppThemeDisplay  {
  final double height; // = Colors.black;

  final bool isSamllHeight;

  const AppThemeDisplay({this.height = 0, this.isSamllHeight = false});
}
